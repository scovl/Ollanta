{
  "key": "go:md5-used-as-password",
  "name": "MD5 Used for Password Hashing",
  "description": "MD5 is cryptographically broken and must not be used for password hashing or any security-sensitive checksum.",
  "language": "go",
  "type": "vulnerability",
  "severity": "major",
  "tags": ["security", "cwe-916"],
  "rationale": "MD5 is vulnerable to collision and pre-image attacks. Password hashes must use slow, memory-hard algorithms such as bcrypt, scrypt, or Argon2 to resist brute-force attacks.",
  "noncompliant_code": "import \"crypto/md5\"\n\nhash := md5.Sum([]byte(password))\nstore(hex.EncodeToString(hash[:]))",
  "compliant_code": "import \"golang.org/x/crypto/bcrypt\"\n\nhash, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)\nif err != nil {\n    return err\n}\nstore(string(hash))"
}
