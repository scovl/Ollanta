package testdata

// clean.go — no issues expected from any rule

// Greet says hello.
func Greet(name string) string {
	return "Hello, " + name
}

// Add adds two integers.
func Add(a, b int) int {
	return a + b
}
